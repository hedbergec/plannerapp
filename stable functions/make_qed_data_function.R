make_qed_data <- function( #create set of schools
  nlow, #students per school low
  nhi, #students per school hi
  mt, #schools in T
  mc, #schools in C set
  mean_x, #average of X in comparison schools
  t_diff_x, #difference between T and all of C in normal X mean
  p_z, #proportion z in comparison schools
  t_diff_z #difference between T and all of C in dummy Z p
) {
  c_clusters <- seq(1:mc)
  c_list <- list()
  for (j in c_clusters) {
    
  }
  
}

