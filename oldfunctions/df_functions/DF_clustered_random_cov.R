

DF_clustered_random_cov <- function(dparms) { #pulls info from dparms and produces the degress of freedom
  list2env(dparms, envir=environment()) 
  result <- mt + mc - 2 - q 
  
  return(result)
  
}

