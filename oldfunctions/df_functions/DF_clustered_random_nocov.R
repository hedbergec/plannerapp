

DF_clustered_random_nocov <- function(dparms) { #pulls info from dparms and produces the degress of freedom
  list2env(dparms, envir=environment()) 
  result <- mt + mc - 2 
  
  return(result)
  
}

